diff --git a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNMapView.m b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNMapView.m
index 28c79d5..a04c505 100644
--- a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNMapView.m
+++ b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNMapView.m
@@ -102,6 +102,7 @@ - (void)notifyStyleLoaded {
 }
 
 - (void)addToMap:(id<RCTComponent>)subview {
+  if (subview == nil) return;
   if ([subview isKindOfClass:[MLRNSource class]]) {
     MLRNSource *source = (MLRNSource *)subview;
     source.map = self;
@@ -138,6 +139,7 @@ - (void)addToMap:(id<RCTComponent>)subview {
 }
 
 - (void)removeFromMap:(id<RCTComponent>)subview {
+  if (subview == nil) return;
   if ([subview isKindOfClass:[MLRNSource class]]) {
     MLRNSource *source = (MLRNSource *)subview;
     source.map = nil;
@@ -203,14 +205,17 @@ - (void)setSourceVisibility:(BOOL)visible
 #pragma clang diagnostic push
 #pragma clang diagnostic ignored "-Wobjc-missing-super-calls"
 - (void)insertReactSubview:(id<RCTComponent>)subview atIndex:(NSInteger)atIndex {
+  if (subview == nil) return;
   [self addToMap:subview];
-  [_reactSubviews insertObject:(UIView *)subview atIndex:(NSUInteger)atIndex];
+  NSUInteger safeIndex = MIN((NSUInteger)atIndex, _reactSubviews.count);
+  [_reactSubviews insertObject:(UIView *)subview atIndex:safeIndex];
 }
 #pragma clang diagnostic pop
 
 #pragma clang diagnostic push
 #pragma clang diagnostic ignored "-Wobjc-missing-super-calls"
 - (void)removeReactSubview:(id<RCTComponent>)subview {
+  if (subview == nil) return;
   // similarly, when the children are being removed we have to do the appropriate
   // underlying mapview action here.
   [self removeFromMap:subview];
diff --git a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNPointAnnotation.m b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNPointAnnotation.m
index 7552a07..fe22c52 100644
--- a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNPointAnnotation.m
+++ b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNPointAnnotation.m
@@ -19,6 +19,7 @@ - (id)init {
 }
 
 - (void)insertReactSubview:(UIView *)subview atIndex:(NSInteger)atIndex {
+  if (subview == nil) return;
   if ([subview isKindOfClass:[MLRNCallout class]]) {
     self.calloutView = (MLRNCallout *)subview;
     self.calloutView.representedObject = self;
@@ -36,7 +37,7 @@ - (void)removeReactSubview:(UIView *)subview {
 }
 
 - (void)reactSetFrame:(CGRect)frame {
-  if ([_map.annotations containsObject:self]) {
+  if (_map != nil && [_map.annotations containsObject:self]) {
     [_map removeAnnotation:self];
   }
   [super reactSetFrame:frame];
@@ -173,10 +174,17 @@ - (void)_updateCoordinate {
 }
 
 - (void)_addAnnotation {
+  if (_map == nil) return;
   if ([_map.annotations containsObject:self]) {
     return;
   }
 
+  // Guard against invalid coordinates (NaN/Inf) that cause native crash
+  CLLocationCoordinate2D coord = self.coordinate;
+  if (!CLLocationCoordinate2DIsValid(coord) || isnan(coord.latitude) || isnan(coord.longitude)) {
+    return;
+  }
+
   [_map addAnnotation:self];
   if (_reactSelected) {
     [_map selectAnnotation:self animated:NO completionHandler:nil];
diff --git a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNShapeSource.m b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNShapeSource.m
index 35804c0..3a7171f 100644
--- a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNShapeSource.m
+++ b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNShapeSource.m
@@ -64,9 +64,9 @@ - (nullable MLNSource *)makeSource {
     options[MLNShapeSourceOptionClusterRadius] = _clusterRadius;
   }
 
-  if (_clusterMinPoints != nil) {
-    options[MLNShapeSourceOptionClusterMinPoints] = _clusterMinPoints;
-  }
+//  if (_clusterMinPoints != nil) {
+//    options[MLNShapeSourceOptionClusterMinPoints] = _clusterMinPoints;
+//  }
 
   if (_clusterMaxZoomLevel != nil) {
     options[MLNShapeSourceOptionMaximumZoomLevelForClustering] = _clusterMaxZoomLevel;
diff --git a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNVectorSource.m b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNVectorSource.m
index 93b995b..2225099 100644
--- a/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNVectorSource.m
+++ b/node_modules/@maplibre/maplibre-react-native/ios/MLRN/MLRNVectorSource.m
@@ -4,7 +4,7 @@ @implementation MLRNVectorSource
 
 - (nullable MLNSource *)makeSource {
   if (self.url != nil) {
-    return [[MLNVectorTileSource alloc] initWithIdentifier:self.id configurationURLString:self.url];
+    return [[MLNVectorTileSource alloc] initWithIdentifier:self.id configurationURL:[NSURL URLWithString:self.url]];
   }
   return [[MLNVectorTileSource alloc] initWithIdentifier:self.id
                                         tileURLTemplates:self.tileUrlTemplates
diff --git a/node_modules/@maplibre/maplibre-react-native/maplibre-react-native.podspec b/node_modules/@maplibre/maplibre-react-native/maplibre-react-native.podspec
index 4eae50f..adeaf33 100644
--- a/node_modules/@maplibre/maplibre-react-native/maplibre-react-native.podspec
+++ b/node_modules/@maplibre/maplibre-react-native/maplibre-react-native.podspec
@@ -35,30 +35,6 @@ def $MLRN._add_spm_to_target(project, target, url, requirement, product_name)
 end
 
 def $MLRN.post_install(installer)
-  spm_spec = $MLRN_SPM_SPEC
-
-  project = installer.pods_project
-  self._add_spm_to_target(
-    project,
-    project.targets.find { |t| t.name == "maplibre-react-native"},
-    spm_spec[:url],
-    spm_spec[:requirement],
-    spm_spec[:product_name]
-  )
-
-  installer.aggregate_targets.group_by(&:user_project).each do |project, targets|
-    targets.each do |target|
-      target.user_targets.each do |user_target|
-        self._add_spm_to_target(
-          project,
-          user_target,
-          spm_spec[:url],
-          spm_spec[:requirement],
-          spm_spec[:product_name]
-        )
-      end
-    end
-  end
 end
 
 Pod::Spec.new do |s|
@@ -73,6 +49,7 @@ Pod::Spec.new do |s|
 
   s.dependency 'React-Core'
   s.dependency 'React'
+  s.dependency 'MapLibre', '6.9.0'
 
   s.subspec 'DynamicLibrary' do |sp|
     sp.source_files	= "ios/MLRN/**/*.{h,m}"
